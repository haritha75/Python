import requests

response = requests.get("http://google.com")
print(response)

# virtual environment is used to use locally package version you can  virtual environment 
# till now pip and virtual envirnemnt used in seperate to use together use need pipenv tool

